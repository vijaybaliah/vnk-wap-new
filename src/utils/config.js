module.exports = Object.assign({
  host: process.env.HOST || 'localhost',
  port: process.env.PORT,
  // beHost: process.env.BEHOST || '',
  apiHost: 'http://localhost',
  // bePrivateHost: process.env.BE_PRIVATE_HOST,
  // redisHost: process.env.REDISHOST || 'localhost',
  // redisNameSpace: process.env.REDISNAMESPACE,
  // // Kindly note web and wap are using same tracking id
  // webGaUID: process.env.WEBGAUID,
  // lang: process.env.LANG,
  // fbAppID: process.env.FB_APP_ID,
});
